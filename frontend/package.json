{
  "name": "pol",
  "version": "0.0.0",
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "@tsconfig/svelte": "^5.0.2",
    "svelte": "^4.2.12",
    "svelte-check": "^3.6.7",
    "tslib": "^2.6.2",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port $(env | grep POL_FRONT_PORT | cut -b 16-19)",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "cy:open": "cypress open",
    "cy:run": "cypress run"
  },
  "type": "module",
  "dependencies": {
    "cypress": "^13.13.1"
  }
}
